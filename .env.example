# MoonTV 环境变量配置
# 请复制此文件为 .env.local 并填入实际值

# ============ 访问控制 ============
# 设置访问密码（必须配置，否则站点将显示警告页面）
PASSWORD=your_password_here

# ============ 存储配置 ============
# 存储类型: localstorage, redis, upstash, d1
NEXT_PUBLIC_STORAGE_TYPE=localstorage

# ============ Redis 配置 (仅在 storage_type=redis 时需要) ============
# REDIS_HOST=
# REDIS_PORT=
# REDIS_PASSWORD=

# ============ Upstash 配置 (仅在 storage_type=upstash 时需要) ============
# NEXT_PUBLIC_UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# ============ D1 配置 (仅在 storage_type=d1 时需要) ============
# NEXT_PUBLIC_D1_DATABASE_ID=
# NEXT_PUBLIC_D1_ACCOUNT_ID=
# D1_API_TOKEN=

# ============ Server酱配置 (可选，用于推送通知) ============
# SCTKEY=
